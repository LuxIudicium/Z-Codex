namespace ZCodex.Core.Models;

// Genre du personnage — ne sert qu'à choisir la variante de skin d'armure affichée (H/F).
// Sans incidence sur les stats ou le code de template GW1. Défaut = Male.
public enum Gender
{
    Male = 0,
    Female = 1,
}
