<UserControl x:Class="ZCodex.App.Controls.SkillSlotControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="64" Height="64">
    <Border BorderBrush="{DynamicResource SlotBorderBrush}" BorderThickness="1" Background="{DynamicResource SlotBackgroundBrush}" CornerRadius="2">
        <Grid>
            <TextBlock Text="?" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource IconMutedBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Image x:Name="SkillIcon" Stretch="Uniform" Margin="2"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"/>
            <TextBlock x:Name="EmptyMark" Text="?" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource IconMutedBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Border>
</UserControl>
